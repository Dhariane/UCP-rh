cd rh-back-end
# Créer un environnement virtuel
python -m venv venv

# Activer l'environnement virtuel
# Windows :
venv\Scripts\activate
# Linux / Mac :
source venv/bin/activate

# Créer un projet Django
django-admin startproject config .

# creer une application api
python manage.py startapp api

#Pour creer une migration
python manage.py migrate

#Pour migreer
python manage.py makemigrations

#Pour installer postgres
pip install psycopg2-binary

#Pour installer Jwt
pip install PyJWT

#Pour installer Pytz
pip install pytz

#Pour installer django
pip install django

#Pour installer djangoRest
pip install djangorestframework

pip install django-cors-headers

pip install Pillow

python manage.py migrate --fake-initial users


cd rh-back-end
# Créer un environnement virtuel
python -m venv venv
 #linux 
    python3 -m venv venv

# Activer l'environnement virtuel
# Windows :
venv\Scripts\activate
# Linux / Mac :
source venv/bin/activate

# Créer un projet Django
django-admin startproject config .

# creer une application api
python manage.py startapp api

#Pour creer une migration
python manage.py migrate

#Pour migreer
python manage.py makemigrations

#Pour installer postgres
pip install psycopg2-binary

#Pour installer Jwt
pip install PyJWT

#Pour installer Pytz
pip install pytz

#Pour installer django
pip install django

#Pour installer djangoRest
pip install djangorestframework

pip install django-cors-headers

pip install Pillow

python manage.py migrate --fake-initial users

#Avant de cree .env 
    pip install python-dotenv

#Pour voir votre SECRET_KEY 
    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

cd rh-back-end
# Créer un environnement virtuel
python -m venv venv
 #linux 
    python3 -m venv venv

# Activer l'environnement virtuel
# Windows :
venv\Scripts\activate
# Linux / Mac :
source venv/bin/activate

# Créer un projet Django
django-admin startproject config .

# creer une application api
python manage.py startapp api

#Pour creer une migration
python manage.py migrate

#Pour migreer
python manage.py makemigrations

#Pour installer postgres
pip install psycopg2-binary

#Pour installer Jwt
pip install PyJWT

#Pour installer Pytz
pip install pytz

#Pour installer django
pip install django

#Pour installer djangoRest
pip install djangorestframework

pip install django-cors-headers

pip install Pillow

python manage.py migrate --fake-initial users

#Avant de cree .env 
    pip install python-dotenv

#intaller Resend
 -pip install resend

#Pour voir votre SECRET_KEY 
    python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
=======
>>>>>>> dcc45aed (linux)
commande gmail: https://myaccount.google.com/ validation en deux etape actiber / => mots de passe application
>>>>>>> 2b8eb512 (deploie)
